{
  "name": "@absolutejs/audit",
  "version": "0.2.3",
  "description": "Cross-surface audit-event substrate for the AbsoluteJS ecosystem — append-only event log with pluggable sinks, optional hash-chain tamper-evidence, and live-wire helpers that thread sync mutations, queue jobs, runtime exits, and secret rotations into one unified log.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/absolutejs/audit.git"
  },
  "homepage": "https://github.com/absolutejs/audit",
  "bugs": {
    "url": "https://github.com/absolutejs/audit/issues"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "license": "BSL-1.1",
  "author": "Alex Kahn",
  "absolutejs": {
    "manifestContract": 2
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "audit",
    "audit-log",
    "compliance",
    "tamper-evident",
    "hash-chain",
    "siem",
    "absolutejs",
    "soc2"
  ],
  "scripts": {
    "build": "rm -rf dist && bun build src/index.ts src/manifest.ts --outdir dist --sourcemap --target=bun --external @absolutejs/handoff --external @absolutejs/manifest --external @sinclair/typebox && tsc --project tsconfig.build.json && absolute-manifest emit",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write \"./**/*.{ts,json,md}\"",
    "verify-package": "absolute-manifest verify-package",
    "check:package": "bun run typecheck && bun run verify-package && bun run build && bun run test && absolute-changelog check",
    "release": "bun run format && bun run check:package && bun publish",
    "prepublishOnly": "bun run check:package"
  },
  "dependencies": {
    "@absolutejs/handoff": "0.1.0",
    "@absolutejs/manifest": "^0.9.0",
    "@sinclair/typebox": "^0.34.0"
  },
  "devDependencies": {
    "@absolutejs/changelog": "^0.6.0",
    "@types/bun": "1.3.14",
    "prettier": "3.5.3",
    "typescript": "5.8.3"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./manifest": {
      "types": "./dist/manifest.d.ts",
      "import": "./dist/manifest.js",
      "default": "./dist/manifest.js"
    },
    "./manifest.json": "./dist/manifest.json"
  },
  "files": [
    "CHANGELOG.md",
    "LICENSE",
    "README.md",
    "changelog.json",
    "dist"
  ]
}
